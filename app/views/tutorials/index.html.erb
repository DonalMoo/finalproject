<p id="notice"><%= notice %></p>

      <div class="container">
        <div class="row">
          <div class="col-xs-2">
            <h3>Tutorials</h3>
            <ul>
              <li>Bodhran</li>
              <li>Tin Whistle</li>
              <li>Tenor Banjo</li>
              <li>Mandolin</li>
              <li>Irish Guitar</li>
            </ul>
          </div>
          <div class ="col-xs-10">
            <div class="jumbotron">
              <h1>Tutorials</h1>
              <p>choose from a wide range of tutorials available, with previews, buy or enter your voucher code to gain access to full videos</p>
              </div>
              </div>
            <% @tutorials.each do |tutorial| %>
                  <div class="col-xs-2">
                    <h3><%= tutorial.title %></h3>
                      <%= image_tag(tutorial.image_url, :class => "list_image") %>
                  </div>
                  <div class="col-xs-8">
                  <%= sanitize(tutorial.description) %>
                  <h4><%= number_to_currency(tutorial.price, unit: "€") %></h4> 
                  <h4>Video Privew</h4>  
                  <%= video_tag(tutorial.preview_url, :size => "320x240", :controls => true) %>

                  <% if current_user.admin? %>
                  <td><%= link_to 'Show', tutorial %></td>
                  <td><%= link_to 'Edit', edit_tutorial_path(tutorial) %></td>
                  <td><%= link_to 'Destroy', tutorial, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                  <% end %>
                  </div>
                </div>
              </div>
              </div>
              </div>
             <% end %>
         </div>
      </div>
  </div>
<br>
<% if current_user.admin? %>
<%= link_to 'New Tutorial', new_tutorial_path %>
<% end %>
